{
  "name": "monitor-cli-task-observer",
  "version": "1.0.15",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build:installable": "npm run -w @monitor/contracts build && npm run -w @monitor/cli build && npm run -w monitor-cursor-bridge build",
    "build:runtime-bundle": "bash ./scripts/build-runtime-bundle.sh",
    "install:local": "bash ./scripts/install.sh",
    "test": "npm run -ws test",
    "build": "npm run -ws build",
    "lint": "npm run -ws lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
